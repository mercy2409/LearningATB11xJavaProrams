package Tasks;

public class Task_002_Math_Functions_12_03 {
    public static void main(String[] args) {
// The given mathematical expression is: ( 𝑥 2 + 𝑦 2 − ∣ 𝑧 ∣ ) 3 3 (x 2 +y 2 −∣z∣) ​
        //x = 10;
        //y = 10;
       // z = 10;
       // int p =2;
        // In Java, Math.cbrt() is a built-in method used to calculate the cube root of a given number.
        //Returns the cube root of the given number as a double.

        // base: The number to be raised to a power.
        //exponent: The power to which the base is raised.
        //Returns: double, the result of base exponent

       // double result = Math.cbrt(Math.pow(x, 2)) + Math.pow(y, 2) - Math.abs(z));
       // System.out.println("Results:" + result);

    }
}
